---
name: 自学汉字数学
description: 开发与迭代「汉字 · 数学 大冒险」——给老曾两个儿子的自学 web 应用：🚀**大宝曾麟轩（小学二年级）**主用数学（11 个关卡：100 以内加减/表内乘除/有余数除法/混合运算/口诀抽背/比大小/列竖式/单位换算/应用题，题目**现场随机生成不存题库**，配小棒·点阵·盘子·十格阵等看得见的图）+ 部编版二年级汉字 **1128 个不重复字**（2025 年秋修订版）（按上/下册 × 单元 × 写字表/识字表分组，认字闯关三题型：看拼音选字/看字选拼音/看字选词）+ ✍️田字格描红写字 + 👆**点字注音**（任何地方不认识的字点一下 → 弹拼音气泡 + 朗读，多音字按词定音）；🐣**二宝曾强（3 岁半·幼儿园小班）**用数一数/比多少/认形状/找规律/5 与 10 以内加减 + 76 个生活主题汉字。单文件 index.html，纯前端 + localStorage，零外部依赖，离线可玩（sw.js）。🔴 老曾 2026-09-18 的硬要求：所有内容必须**场景化、图片化、游戏化**（"这样他们更容易产生兴趣和好记忆"）。触发场景：想新增/优化这个 App 的关卡与字表、加游戏化激励、改视觉文案、修出题逻辑、或部署到 GitHub Pages 分享。每次改完都要：①更新本地 skill 文件 ②commit 并 push 到 GitHub。
---

# 自学汉字数学 · 「汉字 · 数学 大冒险」

一个给两个小孩用的**汉字 + 数学**自学 App。单文件、纯前端、离线可用。

## 当前形态

| | 🚀 大宝 **曾麟轩**（小学二年级） | 🐣 二宝 **曾强**（3 岁半 · 幼儿园小班） |
|---|---|---|
| 数学 | **11 个关卡**（见下表） | 7 个关卡：数一数 / 比多少 / 认形状 / 找规律 / 5 以内加法 / 10 以内加法 / 10 以内减法 |
| 汉字 | 部编版二年级上下册（**2025 年秋修订版**），**1128 个不重复的字**，按「册 → 单元 → 写字表/识字表」分组 | **76 个字、7 个生活主题**（我的身体 / 大自然 / 动物朋友 / 我的家 / 数一数 / 大和小 / 动一动） |
| 关卡数 | 11 | 7 |
| 👆 点字注音 | 两边都有（点任何汉字 → 拼音气泡 + 朗读） | 两边都有 |

- **单文件** `index.html`（约 217 KB），纯前端，无构建步骤（对使用者而言）、无后端、零外部引用（无 CDN、无外链字体）。
- 这个文件就是本 skill 仓库的根，也是 GitHub Pages 的入口。
- 进度按小朋友分开存，`localStorage` key = **`hm_state_v1`**；两个孩子的星星、打卡、错题本互不影响。
- 离线：`sw.js`（stale-while-revalidate）。**改完 App 必须顶 `sw.js` 里的 `VERSION`**，否则缓存永远不清、孩子一直看到旧版——`build.sh` 里有一道闸门会在你忘记时叫一声。

### 🔴 老曾定的产品方向（2026-09-18，原话）

> 「我觉得都需要是那种孩子能联想的**场景化、图片化、游戏化**的，这样他们更容易产生兴趣和好记忆」

这是本 App 的第一设计原则，压过一切「代码更简洁」的考虑。任何新关卡/新字表都要先过这一关：**孩子能不能在脑子里看见它？** 具体落法：
- 数学题**不写「3+4=?」了事**——配小棒（SVG 十根一捆）、点阵、盘子、十格阵、数位分解，让他先看见再算。
- 汉字**不给孤儿字**——二宝的字配 emoji 大图；大宝的字给**可点读的组词**（二年级的字多半没法用图表示，**词就是它的场景**）+ 课本身份（如「📚 部编版二年级上册 · 第一单元 · 写字表」）。
- **没例句就不编例句**。宁可显示课本出处，也不糊一句假句子——假句子会教错语感。

## 数学关卡表（`LEVELS`，定义在 `src/04_data_math.js`）

| 大宝（`LEVELS.da.math`，11 关） | 生成器 | 题数 |
|---|---|---|
| ➕ 100 以内加法（进位） | `add100` | 10 |
| ➖ 100 以内减法（退位） | `sub100` | 10 |
| ✖️ 表内乘法 | `mul` | 10 |
| ➗ 表内除法 | `div` | 10 |
| 🍽️ 有余数除法 | `rem` | 8 |
| 🧮 混合运算（先乘除后加减） | `mix` | 8 |
| 🔢 口诀抽背（填括号） | `mj` | 10 |
| ⚖️ 比大小（填 > < =） | `cmpnum` | 8 |
| 📐 列竖式 | `vert` | 8 |
| 📏 单位换算（米厘米 / 元角分） | `unit` | 8 |
| 📖 应用题 | `word` | 6 |

二宝（`LEVELS.er.math`，7 关）：`count` / `more` / `shape` / `pattern` / `add5` / `add10` / `sub10`，每关 8 题。

### 🔴 数学题的铁律：**现场生成，不存题库**

`GEN` 里每个生成器每调用一次现算一道题。**孩子背不了答案**——这是刻意的，不是偷懒。想加题就加生成器，**不要**去攒一个题目数组。

其余几条踩过坑才有的规矩（都在 `04_data_math.js` 里带注释）：
- **应用题人名分两类**：`WORD_KIDS`（麟轩/曾强/小明/小红/丁丁/冬冬）只能当主角，`WORD_ELDERS`（妈妈/奶奶/外婆/姑姑）只出现在「给他/送他」这一侧。混在一起会出「李老师有 68 元…妈妈又给了他 28 元」这种别扭句子。**涉及「元」的题，主角必须是小朋友；给钱的必须是长辈**（这两条 `gen.test.js` 会逐题验）。
- **不许用代词**（他/她）——二年级孩子读「他有多少个」得回头找先行词，阅读负担全压在算术上。
- **三位数绝不能用 `sticks()` 画小棒**：999 要画 99 捆，几百个 SVG 会把页面卡死（真卡过）。`cmpPic()` 里 100 以上改画数位分解。
- **有余数除法的图按「每 b 个一行」画**，最后一行不满的就是余数，一眼看得出来。答案写成「几……几」，数字键盘为此专门多一个 `……` 键（字号 30px，不放大根本看不清）。

## 汉字数据

### 大宝：`HANZI_G2`（`src/03b_data_g2.js`，**自动生成，不要手改**）

- **真源** `tmp/g2_hanzi.json`；编译脚本 `tools/build_g2.py`；单元主题（歌/课文名 + emoji）来自 `tmp/g2_units.json`，可缺省，**缺了就退化成「第一单元」，绝不编一个主题名糊上去**。
- 数据格式故意用**数组不用对象**：`[汉字, 拼音, 组词1, 组词2]`。1100 多条，省下的每一字节都是孩子手机上少等的一毫秒。
- `xie` = 写字表（要会写✍️），`shi` = 识字表（认得就行👀）。**同一个字可以两处都出现，`pool()` 按字去重**。
- 规模：写字表 500（上 250 / 下 250）· 识字表 909（上 460 / 下 449）· 合计 1409 条 · **去重后 1128 个不同的字**。

**`build_g2.py` 的四道校验**（不合格的字直接丢掉，不进 App）：
1. 必须是**单字**（`len(z)==1`）
2. 拼音非空
3. **至少 2 个组词**
4. **组词必须含本字**（否则是张冠李戴）
5. 同一列表内去重

**`tools/check_g2.js` 生成后再验 6 项**：NFC 规范化 / 声调符号 / 组词含本字 / 单词重复 / **同一单元内不许一个字两个拼音** / 单元结构 / 哨兵字。

> 🔴 **多音字跨单元读音不同是合法的，别去「统一」它**：`炸 zhà` / `zhá`、`量 liàng` / `liáng` 在课本里本来就分单元教。校验只卡「同一单元内」，卡宽了会把课本的正确教法当成错误改掉（真踩过）。

> 🔴 **NFC 规范化**：macOS 上从网页抓来的中文可能是 NFD，直接写进文件会让「诗」在 App 里 `!==` 相等失败。`build_g2.py` 里 `N("…")` 一路贯彻。

### 二宝：`HANZI_QI`（`src/03_data_hanzi.js`，手写）

7 个生活主题、76 个字，每个字带 emoji 大图（**3 岁半的孩子认的是图，不是字**）。

### 认字闯关三题型（`hanziQFactory`，`src/05_app.js`）

- 二宝只有两型：`pic`（看图选字，没图才退化成 `py`）、`py`（看字选拼音）
- 大宝三型：`py`（看字选拼音）、`zi`（看拼音选字）、`ci`（看字选词）

**抽成工厂函数是为了让自动化测试能直接跑几千道题**，跑的就是线上那份代码（测试接缝，不是复制一份）。

🔴 **出干扰项的两个真坑**（都在 `hanziQFactory` 里有注释）：
1. **看字选拼音**的干扰项，光过滤「拼音 ≠ 答案拼音」不够——**两个干扰字自己会撞音**（住 zhù / 助 zhù），渲染出来就是两个一模一样的按钮。必须先 `pyOthers` 从更大池子里挑**拼音两两不同**的干扰项；凑不齐 3 个就降级成 `ci` 题型。
2. **看字选词**的干扰项**必须不含这个字**（含了就也是正确答案）；找不到 3 个就扩到全库 `hanziPool()` 里找。

## 👆 点字注音（2026-09-18 加）

**老曾的要求（原话）**：「曾遴选里面无论是数学还是语文，能不能加一个不认识字点一下出现拼音和朗读的」

**做成什么样**：任何地方（题面、句子、口诀、卡片……）点中一个汉字 → 上面弹出气泡（汉字 + 拼音 + 它所在的词）+ 自动朗读，7 秒后自己消失。**按钮里的字不弹**（按钮有自己的功能，且认字闯关的选项正是要考的东西——弹了就成给答案）。

### 数据：`src/03c_data_pinyin.js`（**生成物，不要手改**）

真源脚本 `tools/build_pinyin.py`（依赖 **pypinyin 0.55.0**），三层：

| 层 | 内容 | 规模 | 作用 |
|---|---|---|---|
| `PY_CHAR` | 字 → 默认读音 | 1651 条 | 兜底。孩子能看到的**每个字**都有 |
| `PY_SEG` | 词/整段 → 逐字拼音 | 219 条 | 按词定音，**多音字的命门** |
| `PY_AMB` | 多音字名单 | 75 字 | 断言用：改过音的字一个都不能漏在名单外 |

**运行时取音**（`pyFindIn(text, off)`，在 `05_app.js`）：在孩子点中的那个文本节点里，找**盖住该字的、最长的 `PY_SEG` 键**；命中取那一节，否则回落 `PY_CHAR` 单字默认音。点「丁丁的个数是麟轩的」的「数」→ 命中「个数」→ `gè shù`。

**三层默认读音优先级**：课本生字表（**硬闸门**，缺口 <1100 字直接报错退出）> `MANUAL_DEFAULT`（人工定音，每条写理由）> 语料众数。`LOCKED = {'一','不'}`：变调是**说话**的规律，单字给本调。
贯穿全表的取舍：**多音字按词定音；变调/轻声给本调**（跟课本给单字注音的做法一致）。

### 🔴 两道闸门（都**失败即 return 1，不许出片**）

1. **`tools/pinyin_reviewed.txt`（24 条）** —— 「多音字在该词里走默认读音也对」的人工结论，冻结成快照，构建时逐字比对。
2. **`tools/pinyin_default_reviewed.txt`（1197 条）** —— 把「多音字走了默认读音」的**每一处上下文**摊成报告（74 字 / 1197 种上下文，形如 `…每【行】9…`），人读一遍再 `--update-review` 冻结。

> 🔴 **为什么非要有第二道**：pypinyin 和我们的默认读音**一致地错**时，任何自动断言都不会叫。2026-09-18 就是这么漏掉「每行 9 颗糖果」的——「行」在 App 的文字里 22,002/22,009 处都是「排/行」义（háng），单字默认却是 xíng，pypinyin 也判 xíng。**这是唯一看得见的地方**，只能靠人读报告。

### 🔴 这一轮抓到的两个真 bug（都是看图/读报告才发现的，测试全绿）

1. **长键盖死短键**：整段「位数多的那个大」被收成了 `PY_SEG` 键、内含 `shǔ`，运行时永远选最长键 → 人工写对的「位数 wèi shù」被盖死。修法：`build_pinyin.py` 里新增 `manual_pick(seg, i)`，**人工键在整段合并里优先**；并在 `tests/pinyin.test.js` 加不变量「**长短键重叠处必须同音**」（把 bug 放回去会报 9 处冲突，含「个一行」把「行」盖成 xíng）。
2. **默认读音本身错**：见上面「每行」的 háng / xíng。默认改 `háng`（附分子分母注释），并给 `MANUAL_SEG` 补了 xíng 系（旅行/行驶/举行/自行车…）避免误伤。

另外读复核面报告还揪出 4 处：一样长→cháng、两只手→zhī、值得→dé、头发→fà、大夫→dài。

### 🔴 气泡不许压住被点的那个字

`.py-pop` 的上下两态**只用 `top` 表达位置**，`transform` 必须完全相同（`transform-origin` 可换）。踩过的坑：`.below` 换了 transform + `transform:translate(-50%,-100%)` 有 0.16s 过渡 → 弹出那一瞬间气泡正好从旧位置滑过、**压在被点的字上**。位置一律 `top = above ? rect.top-10 : rect.bottom+10+h`。

## 架构

- `src/` 分部拼接，`build.sh` 拼成单文件 `index.html`：`01_head`（含全部 CSS）→ `02_body`（9 个屏幕）→ 一个 `<script>`（`03_data_hanzi` + `03b_data_g2` + `03c_data_pinyin` + `04_data_math` + `05_app`）→ `06_foot`（离线缓存注册）。
- `05_app.js` 是主 IIFE，状态层和界面层都在里面（单机小工具，不值当拆两个 IIFE）。
- 9 个屏幕：选小朋友 / 首页 / 认字卡 / 通用答题 / 限时口算 / 乘法口诀表 / 描红写字 / 星星与进度 / 关卡地图。
- 星星：每关 3 星（3=100%，2=≥80%，1=≥60%）。`levelStars[key]` 只记历史最好，**刷低分不会掉星，也不会重复加星**。

## 工具与测试

| 命令 | 干什么 |
|---|---|
| `bash build.sh` | 拼 `index.html` + 离线缓存版本号闸门 |
| `node tools/check_g2.js` | 生字表 6 项校验（1128 字） |
| `node tests/gen.test.js` | 数学出题引擎 **约 59.8 万项**断言（含「悬空量词」「题目字段↔渲染器」两类结构断言）|
| `node tests/pinyin.test.js` | 拼音表 **44 项**断言（覆盖/合法音节/形状/多音字名单/38 条关键读音逐条钉死/长短键不许冲突/复核快照不许空） |
| `python3 tools/build_pinyin.py` | 重新生成 `03c_data_pinyin.js`；**闸门不过就 return 1 且不重写 JS**（`--update-review` 只在人读完报告后冻结快照） |
| `bash tools/browser_test.sh tools/shots/t-py.js` | 真浏览器里点字：真坐标真事件 → 气泡内容/位置/不压字/按钮不弹/真题面「行 háng」 |
| `bash tools/browser_test.sh tools/shots/t-hanzi-qz.js` | 真浏览器里跑 6000 道认字题的不变量检查 |
| `bash tools/shot.sh tools/shots/<名>.js out/<名>.png` | 无头 Chrome 截图（`SHOT_BUDGET=` 调虚拟时间预算） |
| `python3 tools/build_g2.py` | 从 `tmp/g2_hanzi.json` 重新生成 `03b_data_g2.js` |

### 🔴 `tests/gen.test.js` 的做法：**从题面文字反解运算数，独立重算**

不是「跑一遍看崩不崩」，而是拿正则把题面里的数抠出来（`/(\d+)\s*加\s*(\d+)\s*等于几/`），自己算一遍跟答案比。**这样才验得出「算式和答案对不上」这类真错**——只跑不崩的测试对出题器毫无意义。

### 🔴 截图脚本一律**按名字点，不按序号点**

`tools/shots/_lib.js` 提供 `kid()` / `pickMod(name)` / `pickMap(name)`，`shot.sh` 会自动把它前置到每个驱动脚本前面。**踩过的坑**：原来按 `querySelectorAll('.map-item')[7]` 点，插入「有余数除法」这一个关卡后**全部驱动集体串位**，`07-vert.png` 截出来是「比大小」。

### 🔴 两分钟就能跑完的验收次序

```bash
bash build.sh                                     # 1. 拼装（顺便查版本号闸门 + 两道拼音闸门）
node tools/check_g2.js && node tests/gen.test.js  # 2. 数据与出题引擎
node tests/pinyin.test.js                         # 3. 拼音表
bash tools/browser_test.sh tools/shots/t-hanzi-qz.js  # 4. 认字出题器
bash tools/browser_test.sh tools/shots/t-py.js        # 5. 点字注音（真点真坐标）
bash tools/shot.sh tools/shots/15-hanzi-g2-map.js out/15-hanzi-g2-map.png  # 6. 肉眼看
```

> 🔴 **`tools/browser_test.sh` 的退出码只能看「最后那行判定行」**（脚本自己下的结论）。踩过两次：
> ① 按 `'✅' in out` 判 → 失败时输出里也有一堆 `  ✅ ` 子项，永远 exit 0；
> ② 改按 `'🔴' not in out` 判 → 题目里拿 🔴 当**计数物品**（`🔴🔴🟩🔴🔴`），随机抽到红圈题就假报失败。
> 另外：`tools/shots/01~20*.js` 是**截图驱动**（不写 `#TESTOUT`），**别当断言脚本跑**；真断言脚本只有
> `t-smoke` / `t-hanzi-qz` / `t-py` 三个。

### 🔴 截图驱动**自己不会叫**，所以「点字」截图必须带自检条

`tools/shots/2[1-4]-py-*.js` 末尾都挂了 `expectPop(...)`（在 `_lib.js`）：
**气泡没弹出来就往页面底部糊一条大红横幅**，让看图的人一眼看见"这张图没验到东西"。
截出来是**绿条**才算验过。

踩坑原委（2026-09-18 逐张看图才发现）：22 号原来写的是
`tapCharIn('#learn-info',0) || tapCharIn('#learn-ju',0)`——
① `#learn-info` 里**全是组词按钮**（按设计点按钮不弹气泡，它有自己的活干）；
② 而 `tapCharIn` **失败时也返回非空字符串**，`||` 永远短路 → 兜底一次都没跑过。
结果截出来的那张图**一个气泡都没有**，看着像功能没做，其实只是脚本点错了地方。
**教训两条**：截图脚本没有断言＝假绿的温床；**返回状态串的函数不许用 `||` 做兜底**。

### 🔴 点字注音的测试为什么必须进浏览器

取音靠 `document.caretRangeFromPoint`（「屏幕上的这个点对应哪个字」），node 里没有对应物。
而且驱动的是 `index.html` 里那份**真实现**（`window.__hm.py`），不另抄一套匹配逻辑——抄一遍就是判据分叉。
两个坑：
- 探针**必须放屏幕内**：追加到 `.app` 末尾会落到屏幕外，`caretRangeFromPoint` 对屏幕外坐标返回 `null` → **假绿**。
- 一律走 `document.elementFromPoint` 的**真命中测试**再派发事件，绝不「传个 target 强行派发」把失败糊过去。

**看测试结果要看每一行自己的输出**，别只看末尾汇总——踩过「测试根本没跑起来（漏 require）+ 退出码被 `true` 兜底 + 末尾 echo 报成功」三重假绿灯。

### 🔴 自动化测试全绿 ≠ 孩子能答题——**必须自己看一遍截图**

2026-09-18 一轮里抓到的 6 处问题，**没有一处是测试报出来的**，全是逐张看图看出来的：
「比多少」左右两边什么都没画（数据放进了一个渲染器不认识的字段）、
限时口算题面写着看小棒却没画小棒、「列竖式」确定键在屏幕外、
应用题的数量标注被挤断成两行、比大小的讲解没说是哪一位。
所以**「跑完测试」不算完，`out/` 里的图要逐张看过**。
对应的两条防复发断言已写进 `tests/gen.test.js`：
- **题目字段 ↔ 渲染器白名单**：生成器产出渲染器不认识的 key 就报错（专治"数据生成了却没人渲染"）。
- **悬空量词**：`数字+量词` 后面没跟名词时，量词照样得跟物品走（专治「划掉 3 个 ❌」这种漏网）。
  验证断言有效的方法：**把 bug 故意放回去，看它叫不叫**；再确认它不误伤合法用法（如「平均分给 3 个小朋友」）。

## 已知边界（诚实清单，汇报时照说）

- ✅ **生字表版本**：`tmp/g2_hanzi.json` 已换成 **2025 年秋修订版**（2026-09-18），逐页转录自国家中小学智慧教育平台的官方电子教材。旧 2017 版存为 `tmp/g2_hanzi_2017版存档.json`。
  - 上册写字表 250 / 识字表 460，下册写字表 250 / 识字表 449 —— **上册两个数与课本印刷数字吻合**。
  - 识字表里标蓝的「多音字复习字」按课次剔除（上 22 / 下 15），不剔上册会算成 482。
  - ⚠️ **下册写字表 250 字的拼音是照词语表反推的**，不是课本印刷（官方电子教材那几页拼音层缺失）；上册 250 字拼音是课本原样。
  - ⚠️ 下册识字表 449（课本印 450），差的一个是「咦」——查不到含它的真实两字词，按「宁可不收也不编词」丢掉。
  - ⚠️ 组词只保证「是真实词语且含该字」，没有官方印刷物逐字背书。
- 二年级的字**大部分没有配图**。不为凑「图片化」硬配一张不相干的图——那反而教错联想。用**组词点读**当场景。
- 描红写字只判定「有没有写」，**不判笔顺、不判像不像**，靠孩子自觉多写几遍。
- 汉字朗读用浏览器 `speechSynthesis`，**音色取决于设备**，iOS/安卓/macOS 各不相同。
- ⚠️ **点字注音在按钮上不生效**（**故意的**）：认字闯关的选项、组词小卡按一下有它自己的功能（选中/朗读那个词），不能被抢；而且选项里那些字正是「认字闯关」要考的东西，点一下就出拼音等于直接给答案。孩子做「看拼音选字」题时，**选项里的字读不出来**——这是已知缺口，要补得另想不泄题的办法。
- ⚠️ 拼音是**我们自己按词一条条定的**（`PY_CHAR` 1651 字 + `PY_SEG` 219 条），**不是课本印刷那版**。多音字按词定音；变调/轻声（一/不/呀/啦/哇）一律给**本调**。认字卡片上显示的拼音仍来自课本数据，两边不冲突。
- ⚠️ 孩子点中的位置若落在一个**超过 6 字的整段键**里，气泡只给那个**单字**的音（`isWord` 判据），朗读引擎也可能自己把多音字猜错——不过取到的音本身是对的。
- ⚠️ 气泡弹出时会**短暂遮住相邻那一行**（认字卡上点例句的字，气泡在上方就会盖住组词按钮；点课本出处那行也一样），7 秒后自动消失。**没有去治**：这是"不许压住被点的那个字"的代价，改成躲内容反而会飘。
- 没有账号系统，进度只在这台设备的这个浏览器里。**换设备=从零开始，清浏览器数据=进度没了**。

## 迭代铁律

每次改完**必做**：
1. 更新本地 skill 文件（本目录）
2. `bash build.sh` + 跑上面的验收次序
3. commit 并 push 到 GitHub
4. 若动了「事实」（字表数量、关卡数、教材版本…）→ **全域同步**：先问「动的是哪些事实」→ grep 全库列活文档 → 统一口径照抄。详见记忆 `feedback_fact_change_sync_all_skills`。
