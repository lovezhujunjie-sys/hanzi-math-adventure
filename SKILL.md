---
name: 汉字数学大冒险
description: 开发与迭代「汉字 · 数学 大冒险」——给老曾两个儿子的自学 web 应用：🚀**大宝曾麟轩（小学二年级）**主用数学（11 个关卡：100 以内加减/表内乘除/有余数除法/混合运算/口诀抽背/比大小/列竖式/单位换算/应用题，题目**现场随机生成不存题库**，配小棒·点阵·盘子·十格阵等看得见的图）+ 部编版二年级汉字 **1128 个不重复字**（2025 年秋修订版）（按上/下册 × 单元 × 写字表/识字表分组，认字闯关三题型：看拼音选字/看字选拼音/看字选词）+ ✍️田字格描红写字（有回执/计数/星星/奖章）+ 🧾**错题本能照着重练**（每道题带凭据 `_src`）+ 📊数学每关与认字闯关**分本记账**进学习报告 + 👆**点字注音**（任何地方不认识的字点一下 → 弹拼音气泡 + 朗读，多音字按词定音）；🐣**二宝曾强（3 岁半·幼儿园小班）**用数一数/比多少/认形状/找规律/5 与 10 以内加减 + 76 个生活主题汉字。🎮**游戏乐园·五款小游戏**（2026-09-20 起**进去就能玩**，不再要「学满才给玩」；防沉迷改由**每日上限 + 单次时长上限**兜底，家长页可调。五款＝🔨打地鼠 / 🐍贪吃蛇 / 💎三消配对 / 🧱方块消除 / 🦖小恐龙跳跳，每款都把题目长在玩法里：吃对答案才变长、消掉的是同音字、方块上带着字消行时读、跳起来吃对的答案）+ 👨👩👦家长页（游戏时长设置）。单文件 index.html，纯前端 + localStorage，零外部依赖，离线可玩（sw.js）。🎨 视觉为糖果色儿童向 UI（珊瑚橙渐变按钮 #FF7A59→#FFB03A / 淡蓝天空光晕背景 #F3F7FF / 果冻卡片 / 漂浮摇摆弹跳动画；2026-09-18 融合版把 Claude Code+DeepSeek 那版的全部功能数据植入后换的装，答对绿 pop、答错红 shake）。🔴 老曾 2026-09-18 的硬要求：所有内容必须**场景化、图片化、游戏化**（"这样他们更容易产生兴趣和好记忆"）。触发场景：想新增/优化这个 App 的关卡与字表、加游戏化激励、改视觉文案、修出题逻辑、或部署到 GitHub Pages 分享。每次改完都要：①更新本地 skill 文件 ②commit 并 push 到 GitHub。---

# 汉字数学大冒险

一个给两个小孩用的**汉字 + 数学**自学 App。单文件、纯前端、离线可用。

## 当前形态

| | 🚀 大宝 **曾麟轩**（小学二年级） | 🐣 二宝 **曾强**（3 岁半 · 幼儿园小班） |
|---|---|---|
| 数学 | **11 个关卡**（见下表） | 7 个关卡：数一数 / 比多少 / 认形状 / 找规律 / 5 以内加法 / 10 以内加法 / 10 以内减法 |
| 汉字 | 部编版二年级上下册（**2025 年秋修订版**），**1128 个不重复的字**，按「册 → 单元 → 写字表/识字表」分组 | **76 个字、7 个生活主题**（我的身体 / 大自然 / 动物朋友 / 我的家 / 数一数 / 大和小 / 动一动） |
| 关卡数 | 11 | 7 |
| 👆 点字注音 | 两边都有（点任何汉字 → 拼音气泡 + 朗读） | 两边都有 |
| 写完的字 | ✍️ 描红**有回执有记账**（章 + 计数 + 每 5 字 1 颗星 + 奖章） | 同左 |
| 错题 | 错题本**能照着重练**（每道题带凭据，见下） | 同左 |
| 成绩进报告 | 数学每关 + **认字闯关**各自成一本账，报告里逐关列正确率 | 同左 |

- **单文件** `index.html`（约 271 KB），纯前端，无构建步骤（对使用者而言）、无后端、零外部引用（无 CDN、无外链字体）。
- 这个文件就是本 skill 仓库的根，也是 GitHub Pages 的入口。
- 进度按小朋友分开存，`localStorage` key = **`hm_state_v1`**；两个孩子的星星、打卡、错题本互不影响。
- 离线：`sw.js`（stale-while-revalidate）。**改完 App 必须顶 `sw.js` 里的 `VERSION`**，否则缓存永远不清、孩子一直看到旧版——`build.sh` 里有一道闸门会在你忘记时叫一声。

### 🔴 老曾定的产品方向（2026-09-18，原话）

> 「我觉得都需要是那种孩子能联想的**场景化、图片化、游戏化**的，这样他们更容易产生兴趣和好记忆」

这是本 App 的第一设计原则，压过一切「代码更简洁」的考虑。任何新关卡/新字表都要先过这一关：**孩子能不能在脑子里看见它？** 具体落法：
- 数学题**不写「3+4=?」了事**——配小棒（SVG 十根一捆）、点阵、盘子、十格阵、数位分解，让他先看见再算。
- 汉字**不给孤儿字**——二宝的字配 emoji 大图；大宝的字给**可点读的组词**（二年级的字多半没法用图表示，**词就是它的场景**）+ 课本身份（如「📚 部编版二年级上册 · 第一单元 · 写字表」）。
- **没例句就不编例句**。宁可显示课本出处，也不糊一句假句子——假句子会教错语感。

## 数学关卡表（`LEVELS`，定义在 `src/04_data_math.js`）

| 大宝（`LEVELS.da.math`，11 关） | 生成器 | 题数 |
|---|---|---|
| ➕ 100 以内加法（进位） | `add100` | 10 |
| ➖ 100 以内减法（退位） | `sub100` | 10 |
| ✖️ 表内乘法 | `mul` | 10 |
| ➗ 表内除法 | `div` | 10 |
| 🍽️ 有余数除法 | `rem` | 8 |
| 🧮 混合运算（先乘除后加减） | `mix` | 8 |
| 🔢 口诀抽背（填括号） | `mj` | 10 |
| ⚖️ 比大小（填 > < =） | `cmpnum` | 8 |
| 📐 列竖式 | `vert` | 8 |
| 📏 单位换算（米厘米 / 元角分） | `unit` | 8 |
| 📖 应用题 | `word` | 6 |

二宝（`LEVELS.er.math`，7 关）：`count` / `more` / `shape` / `pattern` / `add5` / `add10` / `sub10`，每关 8 题。

### 🔴 数学题的铁律：**现场生成，不存题库**

`GEN` 里每个生成器每调用一次现算一道题。**孩子背不了答案**——这是刻意的，不是偷懒。想加题就加生成器，**不要**去攒一个题目数组。

其余几条踩过坑才有的规矩（都在 `04_data_math.js` 里带注释）：
- **应用题人名分两类**：`WORD_KIDS`（麟轩/曾强/小明/小红/丁丁/冬冬）只能当主角，`WORD_ELDERS`（妈妈/奶奶/外婆/姑姑）只出现在「给他/送他」这一侧。混在一起会出「李老师有 68 元…妈妈又给了他 28 元」这种别扭句子。**涉及「元」的题，主角必须是小朋友；给钱的必须是长辈**（这两条 `gen.test.js` 会逐题验）。
- **不许用代词**（他/她）——二年级孩子读「他有多少个」得回头找先行词，阅读负担全压在算术上。
- **三位数绝不能用 `sticks()` 画小棒**：999 要画 99 捆，几百个 SVG 会把页面卡死（真卡过）。`cmpPic()` 里 100 以上改画数位分解。
- **有余数除法的图按「每 b 个一行」画**，最后一行不满的就是余数，一眼看得出来。答案写成「几……几」，数字键盘为此专门多一个 `……` 键（字号 30px，不放大根本看不清）。

## 汉字数据

### 大宝：`HANZI_G2`（`src/03b_data_g2.js`，**自动生成，不要手改**）

- **真源** `tmp/g2_hanzi.json`；编译脚本 `tools/build_g2.py`；单元主题（歌/课文名 + emoji）来自 `tmp/g2_units.json`，可缺省，**缺了就退化成「第一单元」，绝不编一个主题名糊上去**。
- 数据格式故意用**数组不用对象**：`[汉字, 拼音, 组词1, 组词2]`。1100 多条，省下的每一字节都是孩子手机上少等的一毫秒。
- `xie` = 写字表（要会写✍️），`shi` = 识字表（认得就行👀）。**同一个字可以两处都出现，`pool()` 按字去重**。
- 规模：写字表 500（上 250 / 下 250）· 识字表 909（上 460 / 下 449）· 合计 1409 条 · **去重后 1128 个不同的字**。

**`build_g2.py` 的四道校验**（不合格的字直接丢掉，不进 App）：
1. 必须是**单字**（`len(z)==1`）
2. 拼音非空
3. **至少 2 个组词**
4. **组词必须含本字**（否则是张冠李戴）
5. 同一列表内去重

**`tools/check_g2.js` 生成后再验 6 项**：NFC 规范化 / 声调符号 / 组词含本字 / 单词重复 / **同一单元内不许一个字两个拼音** / 单元结构 / 哨兵字。

> 🔴 **多音字跨单元读音不同是合法的，别去「统一」它**：`炸 zhà` / `zhá`、`量 liàng` / `liáng` 在课本里本来就分单元教。校验只卡「同一单元内」，卡宽了会把课本的正确教法当成错误改掉（真踩过）。

> 🔴 **NFC 规范化**：macOS 上从网页抓来的中文可能是 NFD，直接写进文件会让「诗」在 App 里 `!==` 相等失败。`build_g2.py` 里 `N("…")` 一路贯彻。

### 二宝：`HANZI_QI`（`src/03_data_hanzi.js`，手写）

7 个生活主题、76 个字，每个字带 emoji 大图（**3 岁半的孩子认的是图，不是字**）。

### 认字闯关三题型（`hanziQFactory`，`src/05_app.js`）

- 二宝只有两型：`pic`（看图选字，没图才退化成 `py`）、`py`（看字选拼音）
- 大宝三型：`py`（看字选拼音）、`zi`（看拼音选字）、`ci`（看字选词）

**抽成工厂函数是为了让自动化测试能直接跑几千道题**，跑的就是线上那份代码（测试接缝，不是复制一份）。

🔴 **出干扰项的两个真坑**（都在 `hanziQFactory` 里有注释）：
1. **看字选拼音**的干扰项，光过滤「拼音 ≠ 答案拼音」不够——**两个干扰字自己会撞音**（住 zhù / 助 zhù），渲染出来就是两个一模一样的按钮。必须先 `pyOthers` 从更大池子里挑**拼音两两不同**的干扰项；凑不齐 3 个就降级成 `ci` 题型。
2. **看字选词**的干扰项**必须不含这个字**（含了就也是正确答案）；找不到 3 个就扩到全库 `hanziPool()` 里找。

## 👆 点字注音（2026-09-18 加）

**老曾的要求（原话）**：「曾遴选里面无论是数学还是语文，能不能加一个不认识字点一下出现拼音和朗读的」

**做成什么样**：任何地方（题面、句子、口诀、卡片……）点中一个汉字 → 上面弹出气泡（汉字 + 拼音 + 它所在的词）+ 自动朗读，7 秒后自己消失。**按钮里的字不弹**（按钮有自己的功能，且认字闯关的选项正是要考的东西——弹了就成给答案）。

### 数据：`src/03c_data_pinyin.js`（**生成物，不要手改**）

真源脚本 `tools/build_pinyin.py`（依赖 **pypinyin 0.55.0**），三层：

| 层 | 内容 | 规模 | 作用 |
|---|---|---|---|
| `PY_CHAR` | 字 → 默认读音 | 1651 条 | 兜底。孩子能看到的**每个字**都有 |
| `PY_SEG` | 词/整段 → 逐字拼音 | 219 条 | 按词定音，**多音字的命门** |
| `PY_AMB` | 多音字名单 | 75 字 | 断言用：改过音的字一个都不能漏在名单外 |

**运行时取音**（`pyFindIn(text, off)`，在 `05_app.js`）：在孩子点中的那个文本节点里，找**盖住该字的、最长的 `PY_SEG` 键**；命中取那一节，否则回落 `PY_CHAR` 单字默认音。点「丁丁的个数是麟轩的」的「数」→ 命中「个数」→ `gè shù`。

**三层默认读音优先级**：课本生字表（**硬闸门**，缺口 <1100 字直接报错退出）> `MANUAL_DEFAULT`（人工定音，每条写理由）> 语料众数。`LOCKED = {'一','不'}`：变调是**说话**的规律，单字给本调。
贯穿全表的取舍：**多音字按词定音；变调/轻声给本调**（跟课本给单字注音的做法一致）。

### 🔴 两道闸门（都**失败即 return 1，不许出片**）

1. **`tools/pinyin_reviewed.txt`（24 条）** —— 「多音字在该词里走默认读音也对」的人工结论，冻结成快照，构建时逐字比对。
2. **`tools/pinyin_default_reviewed.txt`（1197 条）** —— 把「多音字走了默认读音」的**每一处上下文**摊成报告（74 字 / 1197 种上下文，形如 `…每【行】9…`），人读一遍再 `--update-review` 冻结。

> 🔴 **为什么非要有第二道**：pypinyin 和我们的默认读音**一致地错**时，任何自动断言都不会叫。2026-09-18 就是这么漏掉「每行 9 颗糖果」的——「行」在 App 的文字里 22,002/22,009 处都是「排/行」义（háng），单字默认却是 xíng，pypinyin 也判 xíng。**这是唯一看得见的地方**，只能靠人读报告。

### 🔴 这一轮抓到的两个真 bug（都是看图/读报告才发现的，测试全绿）

1. **长键盖死短键**：整段「位数多的那个大」被收成了 `PY_SEG` 键、内含 `shǔ`，运行时永远选最长键 → 人工写对的「位数 wèi shù」被盖死。修法：`build_pinyin.py` 里新增 `manual_pick(seg, i)`，**人工键在整段合并里优先**；并在 `tests/pinyin.test.js` 加不变量「**长短键重叠处必须同音**」（把 bug 放回去会报 9 处冲突，含「个一行」把「行」盖成 xíng）。
2. **默认读音本身错**：见上面「每行」的 háng / xíng。默认改 `háng`（附分子分母注释），并给 `MANUAL_SEG` 补了 xíng 系（旅行/行驶/举行/自行车…）避免误伤。

另外读复核面报告还揪出 4 处：一样长→cháng、两只手→zhī、值得→dé、头发→fà、大夫→dài。

### 🔴 气泡不许压住被点的那个字

`.py-pop` 的上下两态**只用 `top` 表达位置**，`transform` 必须完全相同（`transform-origin` 可换）。踩过的坑：`.below` 换了 transform + `transform:translate(-50%,-100%)` 有 0.16s 过渡 → 弹出那一瞬间气泡正好从旧位置滑过、**压在被点的字上**。位置一律 `top = above ? rect.top-10 : rect.bottom+10+h`。

### 🔴🔴 三道闸门：什么时候**不许**弹（2026-09-20 补齐）

老曾 2026-09-20 报：「一点开、或者我换人的时候，冒出一个大字还自动朗读」。同一个症状下藏着**三个**独立缺口，各修各的，**别删其中任何一道**：

1. **开机冷静期 1.5 秒**（`BOOT_AT`）。从微信/桌面点链接打开本页时，浏览器会在加载完成后、于「原触摸坐标」补发一次点击。
   > 🔴 2026-09-18 那道「有没有配套的本页 pointerdown」**挡不住它**——iOS 会把这次补发**合成成完整的一套** pointerdown→pointerup→click（相隔几毫秒）。当时那条修复是**假绿灯**：我的测试用 `fireTap`（成对派发）去验，自然验不出真机上的合成序列。
   - 判据只能是「开机后 1.5 秒内一律不响应」；真人 1.5 秒内连首页都没看清，不会误伤。
   - `BOOT_AT` 取 **`load` 时刻**，不是脚本执行时刻：慢网下加载完成离脚本执行能差一两秒，从脚本执行起算会漏挡。
2. **按下时的屏幕 ≠ 点击时的屏幕 → 不弹**。元素自己的 `onclick` **先跑**、document 上的 listener **后跑**——点一张会切屏的东西（首页卡片、孩子卡）时屏幕已经切走了，这里才拿**旧坐标**去**新屏幕**上取字，取到的是屏幕上完全不相干的另一个字并朗读。
   - 2026-09-20 实测复现：点「游戏乐园」卡片 → 冒出「也」；点「数学游戏」→ 冒出「圆」；点「描一描」→ 冒出「用」。
   - 判据用**按下时的屏幕**（`downScreen`），不要用「目标的 DOM 归属」：`goHome()` 会 `grid.innerHTML=''` 重建节点，按 DOM 归属判会漏；按切屏判既准又稳。
3. **`showScreen()` 必须收气泡**（`pyHide()`）。气泡是 `fixed` 定位、**跨屏幕不消失**的——不收就会漂在新屏幕上 7 秒。老曾截图里首页上那个「音」，就是这么漂上去的。

**回归（改这一块两个都要跑）**：
- `tools/shots/t-ghost.js` —— 切屏误弹。修复前实测复现 **3 例**，修后 0 例。
- `tools/shots/t-py.js` —— 新增「开机冷静期」断言（**必须跑在 1500ms 之前**）；其余 42 项确认没误伤正常点字。注意该脚本的 `run()` 已推到 2400ms（冷静期之后）。

## 🧾 记账与错题本（2026-09-18 加）

### 🔴 每道题出厂时都要挂两张条：`_key`（记哪本账）和 `_src`（错了好重出）

以前这两个字段**都没有**，后果是两件事一起坏掉，而且都是「静默坏」——界面上一点看不出来：

| 缺什么 | 表面症状 | 实际后果 |
|---|---|---|
| `_key` | 答题、结算、星星全都正常 | 成绩全落进 `stats.misc`，**报告里永远看不见汉字和数学的成绩** |
| `_src` | 错题本照样一条条列出来 | 存的是「哪个字读什么？」这种**标签字符串**，题目本身丢了，**想重练都无从下手** |

所以规矩是：**凡是要进错题本的题，出厂时就得带着重新造一份自己的能力**。

```js
q._key = 'hanzi_quiz';                        // 账本名，报告按它取数
q._src = { k: 'hanzi', z: '莓' };              // 凭据：三门中的哪一门 + 足够重出的信息
// 数学：{ k:'math', id:'add100' }   乘法口诀：{ k:'mul' }
```

`rebuildWrong()` 拿凭据重出题：汉字走 `hanziQFactory(pool).one(word)`（**同一个字**的新题），
数学走 `GEN[m.gen]()`（**同一关**的新题——数学题现场生成不存题库，所以永远出不了「原来那道」）。
重练时 `_key` 仍是原来的账本名（`hanzi_quiz` / 关卡号），**绝不许另开一本 `wrong_redo`**，
否则报告里那次练习就成了没主的账。`t-wrongbook.js` 专门盯死这两条。

> 🔴 **错题本的标题必须写出「考的是什么」**，只写「哪个词里有这个字？」等于没写。
> 但**考拼音的题（`py`）标题里出现的是拼音、不是那个字**——字写上去就等于把答案印在题目上。
> 所以 `wrongLabel()` 用的是题面（`big`）而不是答案，测试的判据也是「标题里有字**或**有那行拼音」。

### 揭示条（答错时才出）

孩子「看拼音选字」时选项里的字读不出来（按钮上的点字注音**按设计不生效**，见已知边界）。
补法是**只在答错之后**，在反馈区铺一条「这几个字念什么」：每张卡一个选项的字 + 拼音，答案那张高亮。
- **只在答错时出**：答对还铺一条会拖慢连对的节奏，而且那一刻他并不需要。
- 判断哪张是答案卡、拼音取自哪里，只有一处实现（`revealHtml()` 造卡片 / `bindReveal()` 挂点击），**不许在别处另算一份**。
- 🔴 最容易出的错是**错位**（字按打乱后的顺序、拼音按原顺序算）——那样孩子看到的每一个音都是错的，
  比不做还糟。`t-reveal.js` 逐张交叉核对「字↔音」，词题还要跟**点字注音那条路**（真点坐标）对一遍，
  两条独立路径都说一样才算过。

### 描红的回执

`markTraced()` 里**记账和回执是两件事**：写满一格（笔迹长度 ≥ 1.6 × 格子边长）就**一定**记账；
但**章（盖戳动画 + 彩带 + 音效）按 1.2 秒节流**——描红本来就是同一个字反复写，不节流会刷屏。
- 🔴 **不许有「这个字盖过章就不再管」的一次性开关**。以前有这个 `marked` 标志，孩子重复写同一个字
  会**一点反馈都没有**（写了满满一屏，屏幕上什么都没发生），看起来像坏了。`t-trace.js` 里专门留了一条
  「短划之后接着真描一遍，章照样盖得出来」防它回潮。

### 🔴 写一写的两件保命事（2026-09-20，老曾报「手机上写竖笔顺写不了」）

老曾原话：「**我只要写竖笔顺的时候，我的手机或者iPad就写不了**」，随后他自己给出了正解：
「**或者就是我进入写一写页面，整个就完全锁定不能动，有一锁，这样页面不动才能保证我能正常书写**」。

1. **进去就把整页锁死**（`setPageLock()` + `html.lock-scroll`）：孩子用手指**竖直**往下写时，
   iOS Safari 会把竖向触摸当成**翻页/回弹手势**，页面跟着手指走，竖向笔迹就被整页滚动抢走
   （横着写没事——页面横着不会滚，症状才表现成「只有竖的写不了」）。
   - 锁的规则：`html.lock-scroll{overflow:hidden;height:100%}` + `#screen-trace{touch-action:none}`
     + 运行时总闸 `document.addEventListener('touchmove', …, {passive:false})` 单指一律 `preventDefault`
     （光靠 `overflow:hidden` 挡不住 iOS 的橡皮筋；双指留给缩放，不抢）。
   - 🔴 **锁的前提是「一屏装得下」**：田字格必须按可视高度自己收小（`fitBox()`，294 → 最小 168），
     否则「擦掉重写 / 换一个字」会被顶出屏幕，锁住之后孩子点不到。
   - 🔴 **判「装得下」不能看 `documentElement.scrollHeight`**：锁屏后它被 `height:100%` **夹到视口高度**，
     再高的内容也只报「刚好等于视口」，循环一次都不进（踩过，测试还假通过）。
     要量 `#screen-trace` 里**最后一个元素的下沿**。
   - 离开这一屏（`showScreen`）立刻解锁，别的屏照常能滚。
2. **手指走 `touch` 那条路，不走 pointer**：iOS 遇到滚动/多指会给 `pointercancel`，把笔画掐断。
   所以输入绑成两套：手指 → `touchstart/move/end`（`touchmove` 必须 `{passive:false}`），
   鼠标与触控笔 → `pointer*`。**同一时刻只认先到的来源**（`src`），否则同一根手指被记两遍。
   - 用 `identifier` **认住「正在写的那根手指」**：掌根/第二根手指落下抬起都不许打断这一笔。
   - 鼠标/笔用 `setPointerCapture`，并**删掉 `pointerleave = end`**——以前手指或鼠标一离开格子，
     这一笔就被判结束（画到边上一断，看着就是「写不出来」）。
3. 🔴 **「写完啦」按「盖住了多少字形」判，不按笔迹多长**（2026-09-20 当天第二刀）。
   老曾前一句是「写不了」，下一句是「**整个笔画没有完全写完的时候不要出现写完啦的提示**」——
   两句话一起把判据逼出来了：
   - 只按**笔迹总长**判的毛病：先把门槛定 1.6 倍边长 → 「一」「丨」这种单笔字完整描一遍只有 0.88 倍，
     第一次正确描完**什么都不发生**（看着就是「写不了」）；我把门槛降到 0.6 倍 → 又反过来：
     「意」只描一笔、笔迹够长，也照样盖「写完啦」——**那个提示是假的**，孩子会以为写完了。
   - 现在的判法：① 把要描的字在离屏画布上画一遍取出**字形蒙版**，压成 `GS=24` 的网格（只记有字迹的格）；
     ② 每画一笔就把笔尖经过的格子（按笔宽往四周扩一格）标成"盖到了"；③ 收笔时算覆盖率，
     **≥ `COVER_NEED = 0.72` 才算写完**；④ 交一次账就把覆盖清零，下一遍要重新描满。
   - 效果（`t-trace.js` 两个方向都验）：写一半 → **不盖**；「一」描它唯一那一笔 → 盖；
     「三」只描中间一条 → 不盖（覆盖率约 18%）；整格描一遍 → 盖。
   - 🔴 **仍然不判笔顺、不判像不像、不判好不好**——只看"字形有没有被走过一遍"。这条没变。
   - 探针 `window.__hm.traceCoverage()`（只读）给测试证明"写一半不盖章"不是靠猜。
4. **田字格必须是正方形**：以前拿 `clientWidth` 直接当 height 用，border-box 下内容区被两条边框
   各吃掉 3.5px（画布变 287×280，竖着略扁），现在按内容区反推 wrapper 高度。

## 架构

- `src/` 分部拼接，`build.sh` 拼成单文件 `index.html`：`01_head`（含**主视觉 CSS**：糖果色变量 + 按钮/卡片/田字格/答题/描红等，**第 1 个 `<style>` 块**）→ `02_body`（12 个屏幕 + **第 2 个 `<style>` 块**：关卡地图/combo/点字气泡等）→ 一个 `<script>`（拼接顺序见 `build.sh`：`03_data_hanzi` + `03b_data_g2` + `04_data_math` + `03c_data_pinyin` + `05_app` + `07_games`）→ `06_foot`（离线缓存注册）。🔴 **CSS 分两个 `<style>` 块**（head 一块、body 一块），改视觉两处都要看，详见下面「视觉设计系统」。
- `05_app.js` 是主 IIFE，状态层和界面层都在里面（单机小工具，不值当拆两个 IIFE）。
- 12 个屏幕：选小朋友 / 首页 / 认字卡 / 通用答题 / 限时口算 / 乘法口诀表 / 描红写字 / 星星与进度 / 关卡地图 / 🎮 游戏乐园（游戏中心） / 👨👩👦 家长页 / 游戏进行中。
- 星星：每关 3 星（3=100%，2=≥80%，1=≥60%）。`levelStars[key]` 只记历史最好，**刷低分不会掉星，也不会重复加星**。

## 视觉设计系统（糖果色 UI，2026-09-18 融合版换装）

🔴 **来历**：这版 UI 不是原生长的——2026-09-18 老曾拿来一份 Claude Code + DeepSeek v4.1flash 做的同功能 App，说「他的 UI 很差，UI 还是用你的设计」。于是把**他的全部功能与数据**（就是现在 `src/` 里这套）原样保留，只把**视觉层整个换成糖果色设计系统**。做法＝替换两个 `<style>` 块 + 改 3 处硬编码色（theme-color + 2 处装饰文字色），`build.sh` 产物与老曾已验证的融合版**逐字节一致**（226509 B），`tests/` 全绿不受影响。

**配色变量（都在 `src/01_head.html` 的 `:root`）**：

| 变量 | 值 | 用途 |
|---|---|---|
| `--bg` | `#F3F7FF` | 淡蓝天空背景（body 还叠了两团 radial 光晕） |
| `--brand` / `--brand2` | `#FF7A59` / `#FFB03A` | 珊瑚橙→金黄，主按钮渐变 `--grad-brand` |
| `--blue` | `#5B8DEF` | 大宝主色 / 数学 |
| `--green` | `#2ECC8F` | 答对、进度条（`--grad-green`） |
| `--pink` | `#FF6FB5` | 二宝主色 / 装饰 |
| `--purple` | `#A66CFF` | 强调 |
| `--gold` | `#FFC93C` | 星星、奖章 |
| `--red` | `#FF5C7A` | **答错**（`--grad-red`） |

**招牌动画**：`float`（漂浮）、`wiggle`（摇摆）、`bounce`/`pop`（弹跳）、`shake`（答错抖动）、`fadeUp`（切屏）。主按钮胶囊圆角 50px、卡片果冻圆角 `--r:22px`。

🔴 **三条不能破的视觉规矩**：
1. **答对＝绿、答错＝红**，这是孩子的通用信号，不许用别的颜色表达对错。`.choice.right` 用 `--grad-green` + `bounce`；`.choice.wrong` 用 `--grad-red` + `shake`，与 `.feedback.no` 的红底一致。（融合时踩过坑：`.choice.wrong` 一度用了粉红 `--grad-pink`，粉红读起来像「可爱/中性」而非「答错」，已改回红。）
2. **改 UI 只改 `src/01_head.html` 和 `src/02_body.html` 的两个 `<style>` 块**，别动数据/逻辑 JS；改完 `bash build.sh` 重生 `index.html`，再顶 `sw.js` 的 `VERSION`。
3. 少数**实物色**故意不跟主题走：`src/04_data_math.js` 里「认形状」的圆形 SVG `fill="#ff8a3d"`（一个橙色的圆是实物本身的颜色，不是 UI 铬色，保留）；同文件另 2 处装饰文字色已从 `#ff8a3d` 统一成 `--brand` 的 `#FF7A59`。

## 工具与测试

| 命令 | 干什么 |
|---|---|
| `bash build.sh` | 拼 `index.html` + 离线缓存版本号闸门 |
| `node tools/check_g2.js` | 生字表 6 项校验（1128 字） |
| `node tests/gen.test.js` | 数学出题引擎 **约 59.8 万项**断言（含「悬空量词」「题目字段↔渲染器」两类结构断言）|
| `node tests/pinyin.test.js` | 拼音表 **44 项**断言（覆盖/合法音节/形状/多音字名单/38 条关键读音逐条钉死/长短键不许冲突/复核快照不许空） |
| `python3 tools/build_pinyin.py` | 重新生成 `03c_data_pinyin.js`；**闸门不过就 return 1 且不重写 JS**（`--update-review` 只在人读完报告后冻结快照） |
| `bash tools/browser_test.sh tools/shots/t-py.js` | 真浏览器里点字：真坐标真事件 → 气泡内容/位置/不压字/按钮不弹/真题面「行 háng」 |
| `bash tools/browser_test.sh tools/shots/t-hanzi-qz.js` | 真浏览器里跑 6000 道认字题的不变量检查 |
| `bash tools/shot.sh tools/shots/<名>.js out/<名>.png` | 无头 Chrome 截图（`SHOT_BUDGET=` 调虚拟时间预算） |
| `python3 tools/build_g2.py` | 从 `tmp/g2_hanzi.json` 重新生成 `03b_data_g2.js` |

### 🔴 `tests/gen.test.js` 的做法：**从题面文字反解运算数，独立重算**

不是「跑一遍看崩不崩」，而是拿正则把题面里的数抠出来（`/(\d+)\s*加\s*(\d+)\s*等于几/`），自己算一遍跟答案比。**这样才验得出「算式和答案对不上」这类真错**——只跑不崩的测试对出题器毫无意义。

### 🔴 截图脚本一律**按名字点，不按序号点**

`tools/shots/_lib.js` 提供 `kid()` / `pickMod(name)` / `pickMap(name)`，`shot.sh` 会自动把它前置到每个驱动脚本前面。**踩过的坑**：原来按 `querySelectorAll('.map-item')[7]` 点，插入「有余数除法」这一个关卡后**全部驱动集体串位**，`07-vert.png` 截出来是「比大小」。

### 🔴 两分钟就能跑完的验收次序

```bash
bash build.sh                                     # 1. 拼装（顺便查版本号闸门 + 两道拼音闸门）
node tools/check_g2.js && node tests/gen.test.js  # 2. 数据与出题引擎
node tests/pinyin.test.js                         # 3. 拼音表
BT_BUDGET=200000 bash tools/browser_test.sh tools/shots/t-hanzi-qz.js  # 4. 认字出题器
BT_BUDGET=200000 bash tools/browser_test.sh tools/shots/t-smoke.js     # 5. 所有入口都打得开
BT_BUDGET=200000 bash tools/browser_test.sh tools/shots/t-py.js        # 6. 点字注音（真点真坐标）
BT_BUDGET=260000 bash tools/browser_test.sh tools/shots/t-reveal.js    # 7. 揭示条（字↔音逐张核）
BT_BUDGET=900000 bash tools/browser_test.sh tools/shots/t-trace.js     # 8. 描红回执 + 覆盖率判据（一/三 两个方向）
BT_BUDGET=260000 bash tools/browser_test.sh tools/shots/t-trace-touch.js # 8b. 写一写触屏：整页锁定 + 竖画能写 + 掌根不打断
NODE_PATH=/tmp/pwtest/node_modules node tools/touch_test.js            # 8c. 🔴 真触摸（CDP）三档视口：页面位移=0、田字格自动收小、按钮够得着
BT_BUDGET=200000 bash tools/browser_test.sh tools/shots/t-wrongbook.js # 9. 错题本能练、账没错位
BT_BUDGET=200000 bash tools/browser_test.sh tools/shots/t-games.js     # 10. 游戏乐园（门槛已拆/真吃/防沉迷）
BT_BUDGET=200000 bash tools/browser_test.sh tools/shots/t-games2.js    # 11. 五款游戏都能玩（真点真消真跳）
bash tools/shot.sh tools/shots/15-hanzi-g2-map.js out/15-hanzi-g2-map.png  # 12. 肉眼看
bash tools/shot.sh tools/shots/a3-games-center.js out/a3-games-center.png  # 13. 游戏中心也要肉眼看
```

> 🔴 **`tools/browser_test.sh` 的退出码只能看「最后那行判定行」**（脚本自己下的结论）。踩过两次：
> ① 按 `'✅' in out` 判 → 失败时输出里也有一堆 `  ✅ ` 子项，永远 exit 0；
> ② 改按 `'🔴' not in out` 判 → 题目里拿 🔴 当**计数物品**（`🔴🔴🟩🔴🔴`），随机抽到红圈题就假报失败。
> 另外：`tools/shots/01~3x*.js` 里凡**不写 `#TESTOUT`** 的都是**截图驱动**，别当断言脚本跑。
> 真断言脚本是 `t-` 开头那六份：`t-smoke` / `t-hanzi-qz` / `t-py` / `t-reveal` / `t-trace` / `t-wrongbook`。
>
> 🔴 **`BT_BUDGET` 不能省**：那是 `--virtual-time-budget`，不给的话测试跑到一半 budget 到期就被砍，
> 输出里只剩前半截，看着像"就跑了几行"——**这不是失败，是没跑完**，两种都得看末尾那行判定。

### 🔴 截图驱动**自己不会叫**，所以「点字」截图必须带自检条

`tools/shots/2[1-4]-py-*.js` 末尾都挂了 `expectPop(...)`（在 `_lib.js`）：
**气泡没弹出来就往页面底部糊一条大红横幅**，让看图的人一眼看见"这张图没验到东西"。
截出来是**绿条**才算验过。

踩坑原委（2026-09-18 逐张看图才发现）：22 号原来写的是
`tapCharIn('#learn-info',0) || tapCharIn('#learn-ju',0)`——
① `#learn-info` 里**全是组词按钮**（按设计点按钮不弹气泡，它有自己的活干）；
② 而 `tapCharIn` **失败时也返回非空字符串**，`||` 永远短路 → 兜底一次都没跑过。
结果截出来的那张图**一个气泡都没有**，看着像功能没做，其实只是脚本点错了地方。
**教训两条**：截图脚本没有断言＝假绿的温床；**返回状态串的函数不许用 `||` 做兜底**。

### 🔴 点字注音的测试为什么必须进浏览器

取音靠 `document.caretRangeFromPoint`（「屏幕上的这个点对应哪个字」），node 里没有对应物。
而且驱动的是 `index.html` 里那份**真实现**（`window.__hm.py`），不另抄一套匹配逻辑——抄一遍就是判据分叉。
两个坑：
- 探针**必须放屏幕内**：追加到 `.app` 末尾会落到屏幕外，`caretRangeFromPoint` 对屏幕外坐标返回 `null` → **假绿**。
- 一律走 `document.elementFromPoint` 的**真命中测试**再派发事件，绝不「传个 target 强行派发」把失败糊过去。

**看测试结果要看每一行自己的输出**，别只看末尾汇总——踩过「测试根本没跑起来（漏 require）+ 退出码被 `true` 兜底 + 末尾 echo 报成功」三重假绿灯。

### 🔴 自动化测试全绿 ≠ 孩子能答题——**必须自己看一遍截图**

2026-09-18 一轮里抓到的 6 处问题，**没有一处是测试报出来的**，全是逐张看图看出来的：
「比多少」左右两边什么都没画（数据放进了一个渲染器不认识的字段）、
限时口算题面写着看小棒却没画小棒、「列竖式」确定键在屏幕外、
应用题的数量标注被挤断成两行、比大小的讲解没说是哪一位。
所以**「跑完测试」不算完，`out/` 里的图要逐张看过**。
对应的两条防复发断言已写进 `tests/gen.test.js`：
- **题目字段 ↔ 渲染器白名单**：生成器产出渲染器不认识的 key 就报错（专治"数据生成了却没人渲染"）。
- **悬空量词**：`数字+量词` 后面没跟名词时，量词照样得跟物品走（专治「划掉 3 个 ❌」这种漏网）。
  验证断言有效的方法：**把 bug 故意放回去，看它叫不叫**；再确认它不误伤合法用法（如「平均分给 3 个小朋友」）。

## 已知边界（诚实清单，汇报时照说）

- ✅ **生字表版本**：`tmp/g2_hanzi.json` 已换成 **2025 年秋修订版**（2026-09-18），逐页转录自国家中小学智慧教育平台的官方电子教材。旧 2017 版存为 `tmp/g2_hanzi_2017版存档.json`。
  - 上册写字表 250 / 识字表 460，下册写字表 250 / 识字表 449 —— **上册两个数与课本印刷数字吻合**。
  - 识字表里标蓝的「多音字复习字」按课次剔除（上 22 / 下 15），不剔上册会算成 482。
  - ⚠️ **下册写字表 250 字的拼音是照词语表反推的**，不是课本印刷（官方电子教材那几页拼音层缺失）；上册 250 字拼音是课本原样。
  - ⚠️ 下册识字表 449（课本印 450），差的一个是「咦」——查不到含它的真实两字词，按「宁可不收也不编词」丢掉。
  - ⚠️ 组词只保证「是真实词语且含该字」，没有官方印刷物逐字背书。
- 二年级的字**大部分没有配图**。不为凑「图片化」硬配一张不相干的图——那反而教错联想。用**组词点读**当场景。
- 描红写字只判定「**字形有没有被盖过一遍**」，**不判笔顺、不判像不像**，连「写得对不对」都不看——
  写得再歪也照样盖章（故意的：这个年纪卡对错会打击积极性，多写几遍才是目的）。
  - 🔴 判据沿革（两句话逼出来的）：笔迹长度门槛 1.6 倍 → 0.6 倍 → **2026-09-20 改成字形覆盖率 ≥ 0.72**。
    原因见上面「写一写的两件保命事」第 3 条。**别再改回"按笔迹长度"**。
  - ⚠️ 代价：孩子写这个字**必须真的走遍整个字形**才盖章，写两笔（哪怕很长）不再有回执 ——
    这是老曾点名要的（"没写完不要出现写完啦"）。他要是觉得孩子因此卡住，先问他再放宽。
  - ⚠️ **没验过**：老曾手机上真写一遍（我这边只有触摸模拟，验不了 iOS 真机的橡皮筋行为）；
    以及他孩子实际用不用得顺手。
- 汉字朗读用浏览器 `speechSynthesis`，**音色取决于设备**，iOS/安卓/macOS 各不相同。
- ⚠️ **点字注音在按钮上不生效**（**故意的**）：认字闯关的选项、组词小卡按一下有它自己的功能（选中/朗读那个词），不能被抢；而且选项里那些字正是「认字闯关」要考的东西，点一下就出拼音等于直接给答案。
  - **已部分补上**：答错之后会铺一条「揭示条」，把这一题几个选项的字和拼音一起列出来（答案那张高亮）——那时答案已经揭晓，不算泄题，而且**正好是他最想知道这几个字念什么的那一刻**。答对时不出（不拖慢连对的节奏）。
  - **剩下的缺口仍在**：答题**过程中**（还没选之前）选项里的字依然读不出来。要做只能另想不泄题的办法（比如只读被排除掉的选项），**目前没做**。
- ⚠️ 拼音是**我们自己按词一条条定的**（`PY_CHAR` 1651 字 + `PY_SEG` 219 条），**不是课本印刷那版**。多音字按词定音；变调/轻声（一/不/呀/啦/哇）一律给**本调**。认字卡片上显示的拼音仍来自课本数据，两边不冲突。
- ⚠️ 孩子点中的位置若落在一个**超过 6 字的整段键**里，气泡只给那个**单字**的音（`isWord` 判据），朗读引擎也可能自己把多音字猜错——不过取到的音本身是对的。
- ⚠️ 气泡弹出时会**短暂遮住相邻那一行**（认字卡上点例句的字，气泡在上方就会盖住组词按钮；点课本出处那行也一样），7 秒后自动消失。**没有去治**：这是"不许压住被点的那个字"的代价，改成躲内容反而会飘。
- 没有账号系统，进度只在这台设备的这个浏览器里。**换设备=从零开始，清浏览器数据=进度没了**。
## 🎮 游戏乐园（`src/07_games.js` + `screen-games` / `screen-play` / `screen-parent`）

**🔴 2026-09-20 起：进去就能玩，没有「学满才给玩」的门槛。** 那道门槛已连同 `mode/tier/reward` 一起删掉，别再照着旧描述想当然。

**为什么拆（老曾报「游戏乐园不能玩」查出来的根因）**：旧规则是「学满 15 分钟 → 奖励 10 分钟」，而**二宝 3 岁半根本坐不住 15 分钟**，他的余额永远是 `0:00` —— 游戏乐园对他等于坏的，界面上还看不出来（卡片点得动，进去就被踢回）。而且游戏本身就长在数数/认字/口算上，再锁一道逻辑不通。老曾拍板：**直接能玩 + 每日上限**。

**现在只剩两道硬顶**（`DEFAULT_GAME_CFG`，家长页可调，存 `S.gameCfg`）：

| 档位 | 默认 | 家长页可选 |
|---|---|---|
| ① 每天最多玩多久（防沉迷硬顶） | 30 分钟 | 15 / 20 / 30 / 45 / 60 |
| ② 单次最长（到点提醒休息） | 10 分钟 | 5 / 10 / 15 / 20 |

- `gameRemainSec()` = 每日上限 − 今日已玩（`gameDayRoll()` 跨天清零）；`spendGameSec(n)` 每秒记一次。
- 单次可玩 = `min(sessionMin*60, gameRemainSec())`（`shell()` 里）；走完任一道 → 弹提示并退回游戏中心。
- 家长页现在**只有这两项** + 一句「游戏本身就是学习，所以不再设门槛」。

**五款游戏**（都在 `GAMES` 数组里，`kid: ['da','er']` 两款档案都能玩）：

### 🔴 2026-09-20 追加：先认字 + 每个游戏都能调速度

老曾原话（三句，依次提的）：
1. 「方块消除游戏，玩游戏前要先学习这局方块上方的文字才能玩，而且要加，
   可以点触之后有拼音和朗读，这样他们不认识的字可以看拼音」
2. 「贪吃蛇目前的速度太快了，能不能加一个调速度的」→「打地鼠应该也要有可以调节速度的」
3. 「每个游戏都要可以调节速度的地方吧！最好随时都可以调节速度」

**方块消除：先认字再玩（`tetris` 里的 `.tt-prep` 面板）**

- 开局用一块面板**盖住**棋盘（`#play-stage{position:relative}` + `.tt-prep{position:absolute;inset:0}`），
  把这局要用的 7 个字/数字列出来，**每个点一下**：弹拼音气泡 + 念出来（`b.pyTap()`）。
  认全了「开始玩」才亮。认字这段时间不计游戏时长（`shell()` 的 `run.hold()/resume()`）。
- 🔴 **游戏屏是全局「点字注音」故意关掉的地方**（`bindPyTap` 里那句
  `if (curScreen === 'screen-play') return;`，为的是地鼠头顶/方块上的字被误弹），
  所以游戏里要看拼音必须显式喊 `b.pyTap(z, el, pyOverride)` —— 气泡和读音仍是**同一份**实现。
  `pyOverride` 是给数字用的（1 → yī；拼音表里只有汉字，用 `NUM_PY`）。
- 「盖住」而不是「藏起来」：底下的按钮点不到、分也刷不了，但方块那套逻辑一行都不用挪。

**速度档：五个游戏里能调的都在行里，随时可改（`speedCtl()`，`07_games.js` 顶部）**

| 游戏 | 键 | 调的是什么 | 慢 / 中 / 快 | 默认 |
|---|---|---|---|---|
| 🔨 打地鼠 | `moleSpeed` | 冒头间隔 / 地鼠停留 | 1250·2600 / 1000·2200 / 850·1900 ms | 二宝慢、大宝中 |
| 🐍 贪吃蛇 | `snakeSpeed` | 每走一格毫秒数（原来固定 260） | 460 / 340 / 250 ms | 同上 |
| 🧱 方块消除 | `tetrisSpeed` | 方块下落间隔（原来固定 720） | 900 / 720 / 520 ms | 同上 |
| 🦖 小恐龙 | `dinoSpeed` | 世界滚动速度（原来固定 3.0） | 2.2 / 3.0 / 4.2 像素/帧 | 同上 |
| 💎 三消配对 | —— | **没有可调的**（它本来就没有时间压力，点两下换位，慢慢想） | —— | —— |

- 选择存进 `S.opts.<键>`（跟着进度进 localStorage），下次进来还是那一档；
  老存档没有 `S.opts`，`gameOpt()` 里必须兜底。
- 每档有自己的加速下限（蛇/方块到点会自己变快，但不越过当前档的下限）。
- 🔴 **`stage.innerHTML` 里用到 `SPD.html`，所以 `speedCtl()` 必须在拼 HTML 之前先建好**（踩过 TDZ，
  症状是整个游戏白屏 + 测试拿不到输入框）。

**三消：连锁之后的「读音定格」（老曾：「连着消除之后，读音能不能定格一定的时候，方便小朋友记忆」）**

- 原来每消一步就 `b.speak()` 一次，而连锁每步只隔 ~480ms，`speak()` 会把上一句掐掉 ——
  孩子听到的是零碎的音，字也没看清。
- 现在整条链走完之后才 `holdReading()`：把这一串读音**一次性定格**成一张卡
  （大字 + 拼音 + 进度点），一个字一个字地读，每个停 **1.2~1.8 秒**（按 4.2 秒总预算摊），
  读完自己收走。定格期间手是停的（`resolve()` 还在 `busy` 里），盘面不会在眼皮底下继续动。

| 图标 | 名字 | 玩法 | 学什么 | 二宝版 vs 大宝版 |
|---|---|---|---|---|
| 🔨 | 打地鼠 | 先数洞，再打对的那只 | 数数·口算·认字 | 数字 / 算式·汉字 |
| 🐍 | 贪吃蛇 | 吃对答案才变长，吃错扣一节 | 口算·认字 | 同上 |
| 💎 | 三消配对 | 同组连成三个就消掉 | 认数·同音字 | 4 色 / 拼音分组 |
| 🧱 | 方块消除 | 方块上带着字，消行时读 | 认数·认字 | 7 个数字 / 7 个汉字 |
| 🦖 | 小恐龙跳跳 | 跳起来吃对的答案 | 口算·认字 | 同上 |

**几处刻意的设计（不是随手写的，别改坏）**：
- **三消的「一组」是什么**：二宝版是同色（数字 1–4）；大宝版是**同拼音的不同汉字**（如 hé → 何/盒），组由 `m3Groups()` 扫 `b.hanziPool()` 按拼音现攒，**练的是「见字想音」的眼熟，不是听写**——别指望玩这个就会写。
- **方块消除的汉字是「反复看见」式的眼熟，不是考核**——别说成「玩这个就能认字」。
- **小恐龙的气球长得一模一样**（不靠颜色暗示对错），一屏只来一个，3 个候选按随机的顺序飘过；5 颗心，撞错扣一颗。
- **开局不许有三连**（不然一进去就白送一波）；`findMove()` 用**真 matcher** 暴力扫，扫不到就重排。

**测试口子（`window.HMGames`）**：`_peek/_probe/_steer/_hint/_act` 是只读探针 + 动作；`_clearBoard/_setCell/_setPiece` 是**夹具**——用来「布置输入」，摆完那几行仍然走真实的 `lock → clearLines`。🔴 **这里不许出现「直接把分改高」「直接把行消掉」这种口子**：测试要验的正是那些动作本身，绕过去就等于自己验自己。

**回归（改这一块必须跑）**：
- `tools/shots/t-games.js`（34 项）—— 门槛已拆、进去就能玩、**打地鼠与贪吃蛇的速度档真的改了节奏并被记住**、贪吃蛇**真把蛇引到正确答案那儿**、吃错扣节不换题、每日上限仍然拦得住。
- `tools/shots/t-games2.js`（61 项）—— 五款都在列表里、**方块消除先认字（面板/拼音/门禁/开始）**、**四个游戏的速度档**、**三消连锁后的读音定格**、三消开局无三连 + 真命中测试点方块 + 连锁走干净、方块消除真按钮动作 + 一次消 4 行、小恐龙撞错扣心 + 吃对得分 + 能落地。
  - 跑法 `BT_BUDGET=90000 bash tools/browser_test.sh tools/shots/t-games2.js`（**虚拟时间预算必须盖过脚本里的 sleep 总量**）。
- `NODE_PATH=/tmp/pwtest/node_modules node tools/touch_test.js`（34 项）—— 真浏览器里验**看得见**的那部分：
  写一写的整页锁定/竖画/田字格自适应，和方块消除「先认字」的**拼音淡入（opacity 1）+ 拼音气泡**。
  🔴 这两条不能只靠 `browser_test.sh`：那套带 `--virtual-time-budget`，**CSS 过渡不推进**，
  在那边 opacity 永远是 0（假红）。
- 截图驱动 `a3-games-center.js` / `a4-games-m3.js` / `a5-games-tt.js` / `a6-games-dn.js` / `a7-games-m3-da.js` / `a8-games-tt-da.js` / `a9-parent.js`，🔴 **必须放在 `tools/shots/` 目录里**（`shot.sh` 是从**驱动自己所在的目录**去 prepend `_lib.js` 的，放 `/tmp` 会静默 ReferenceError → 截出来的是选人页）。

## 迭代铁律

每次改完**必做**：
1. 更新本地 skill 文件（本目录）
2. `bash build.sh` + 跑上面的验收次序
3. commit 并 push 到 GitHub
4. 若动了「事实」（字表数量、关卡数、教材版本…）→ **全域同步**：先问「动的是哪些事实」→ grep 全库列活文档 → 统一口径照抄。详见记忆 `feedback_fact_change_sync_all_skills`。
