# 汉字 · 数学 大冒险 —— 使用说明

给麟轩（二年级）和曾强（3 岁半）做的自学 App。
跟「单词大冒险」是同一套形态：单文件网页，点开就能玩，不用装东西。

## 手机上怎么用（推荐）

把这个链接发到微信里，自己点一下，再用手机浏览器打开、加到主屏幕：

**https://lovezhujunjie-sys.github.io/hanzi-math-adventure/**

加到主屏幕之后，图标长得像个 App，点开全屏，**没有网也能玩**（会自动缓存）。

## 电脑上怎么用

直接双击本文件夹里的 `index.html` 就能玩。

## 里面有什么

| | 🧒 麟轩（二年级） | 👶 曾强（3 岁半） |
|---|---|---|
| 数学 | **11 个关卡** | 7 个关卡 |
| 汉字 | 二年级上下册 **1128 个不同的字** | **76 个字**（每个字配一张大图） |

**麟轩这边：**
- 🚀 数学闯关 —— 100 以内加减、乘法口诀、除法、有余数除法、混合运算、比大小、列竖式、单位换算、应用题。**题目是每次现场生成的，没有固定题库，背不了答案。**
- ⏱️ 限时口算 —— 60 秒看能答对几题
- 🔢 乘法口诀 —— 点一句听一句
- 🔤 认汉字 —— 按「册 → 单元 → 写字表/识字表」分好，跟课本一模一样。每个字能听读音、有组词
- ✍️ 写一写 —— 手指在田字格上描红
- 🏆 星星奖章 —— 攒星星
- 👆 **点字注音**（2026-09-18 新加）—— **任何地方**看到不认识的字（题面、句子、口诀、卡片……），
  拿手指**点它一下**：上面弹出拼音气泡 + 自动念出来，7 秒后自己消失。
  多音字按**词**给音：点「数」，它会告诉你这是「个数 gè shù」还是「数一数 shǔ yī shǔ」。
  ⚠️ 只在**能点的按钮上**不生效（见下面「已知的不足」）。

**曾强这边：** 数一数、比多少、认形状、找规律，配 emoji 大图；汉字 7 个生活主题共 76 个字。

数学题都**画了图**：小棒（一捆 10 根）、点阵、盘子分堆、十格阵。孩子能看着图数，不是死记。

## 进度

进度存在**这台设备的这个浏览器**里。换个手机、或者清了浏览器数据，就得从头开始。
目前没有账号系统。

## 已知的不足（这些得说清楚）

- ⚠️ **按钮上的字点不出拼音**，这是**故意**的：认字闯关的选项、组词小卡按一下有它自己的功能
  （选中 / 朗读那个词），不能被抢；而且选项里那些字正是「认字闯关」要考的东西——
  点一下就出拼音等于直接给答案。想在选项上也给提示的话，跟我说，我另想个不泄题的办法。
- ⚠️ 拼音是**我们自己按词一条条定的**（1651 个字的默认读音 + 219 条词语读音），不是课本印刷那版。
  多音字按词定音；变调/轻声（一/不/呀/啦/哇）一律给**本调**——跟课本给单字注音的做法一致。
  认字卡片上显示的拼音仍来自课本数据，两边不冲突。

- ⚠️ **还没有任何真人（孩子）用过**。所有检查都是电脑自动跑的，
  「麟轩觉得难不难」「曾强坐不坐得住」——只有孩子试了才知道。
- ⚠️ 二年级的字大部分**没有配图**。因为这些字（比如「依」「尽」「穷」）本来就没法用一张图表示，
  硬配一张不相干的图反而教错联想。所以改成给**组词**——词就是它的场景。
- ⚠️ 下册写字表 250 个字的拼音是照着词语表反推的，不是课本印刷那版（官方电子教材那几页拼音层缺失）。
- ⚠️ 写字只判「有没有写」，**不判笔顺、不判像不像**，得靠孩子自觉多写几遍。
- ⚠️ 读音用的是浏览器自带的朗读，**音色跟着设备走**，手机和电脑不一样。
- ⚠️ 下册识字表收了 449 个字（课本印的是 450），差的一个是「咦」——
  找不到含它的真实词语，宁可不收也不瞎编。

## 想改哪里

真源在 `~/.agents/skills/汉字数学大冒险/`——**单文件 `index.html`，没有构建步骤**（不像早期版本那样拆 `src/` 再 `build.sh` 拼装），改哪就直接改这一个文件。
改完记得顶一下 `sw.js` 里的 `VERSION`（不顶的话孩子看到的还是缓存里的旧版）。
跟 AI 说一声要改什么就行。
